# Правила `src/shared`

- Только маленькие действительно общие pure utilities и технические типы.
- Не хранить центры, роверы, маршруты, баланс и игровые события.
- Утилита для одного потребителя остаётся рядом с ним.
- Запрещены импорты из `app`, `content`, `domain`, `simulation`, `game`, `ui`.
